# Miku-Memory-Layer Prompts

SUMMARY_PROMPT = """
你是一个专业的对话记录员。你的任务是将下面的对话记录总结为一个简洁、客观的【会话摘要】。

【输入对话】
{context}

【要求】
1. **第三人称**：使用“用户”和“Miku”作为主语。
2. **抓重点**：只记录重要的讨论话题、用户表达的明确观点、发生的关键事件。忽略无意义的寒暄（如“在吗”、“你好”）。
3. **时序感**：如果对话中包含了因果关系（因为A所以B），请保留。
4. **简洁**：摘要长度控制在 50-100 字以内。
5. **格式**：直接输出摘要文本，不要加任何前缀或解释。

【示例】
输入：
User: 显存又不够了，跑不动大模型。
Miku: 确实呢，要不试试量化版本？
User: 试过了，效果不好。我打算把服务搬到另一台服务器上。
Miku: 那是个好主意，Zerotier组网弄好了吗？

输出：
用户抱怨本地显存不足以运行大模型，尝试过量化版本但效果不佳。用户计划将服务迁移至远程服务器，Miku询问了Zerotier组网的进度。
"""

REFLECTION_PROMPT = """
你是一个构建用户画像的专家。你的任务是根据【旧画像】和【最新会话摘要】，更新并完善用户的画像。

【当前画像】
{current_profile}

【最新会话摘要】
{new_summary}

【指令】
1. **分析**：从最新摘要中提取关于用户的**长期事实**（如职业、爱好、设备环境、性格特征、人际关系）。
2. **更新**：
   - 如果有新事实，添加到画像中。
   - 如果与旧画像冲突（例如用户换了工作），以新信息为准进行修正。
   - 如果旧画像中的某些信息已明显过时或不再重要，可以删除。
3. **保持**：不要丢失旧画像中依然准确的重要信息。
4. **风格**：画像应采用**Markdown列表**或**简洁的段落**格式，客观描述，方便AI阅读。

【输出格式】
直接输出更新后的完整画像文本。不要输出“更新如下”等废话。如果没有任何值得更新的内容，请直接输出原画像。
"""
